#!/bin/bash

cd /usr/local/src/
tar xvfz {{ ruby_source_dir }}.tar.gz
cd {{ ruby_source_dir }}
./configure --prefix={{ ruby_location }} --disable-install-doc
make --jobs {{ ansible_processor_cores }}
make install
